<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    >

    <include
        android:id="@+id/msg_left_photo_common_item"
        layout="@layout/msg_common_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/msg_left_photo_common_item"
        android:orientation="horizontal"
        >

        <include
            android:id="@+id/msg_photo_user"
            layout="@layout/msg_user_item"
            android:layout_width="38dp"
            android:layout_height="38dp"
            />

        <RelativeLayout
            android:layout_width="161dp"
            android:layout_height="wrap_content"
            android:padding="4dp">

            <ImageView
                android:id="@+id/image"
                android:layout_width="161dp"
                android:layout_height="114dp"
                android:scaleType="centerCrop"
                />

            <ProgressBar
                android:id="@+id/progressBar"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_centerInParent="true"
                />

            <TextView
                android:id="@+id/progressText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:text="0%"
                android:textColor="#ffffff"/>

        </RelativeLayout>

        <include
            android:id="@+id/msg_left_photo_state"
            layout="@layout/msg_state_item"
            />
    </LinearLayout>
</RelativeLayout>